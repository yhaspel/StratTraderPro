# M10.5 — Execution Report

**Milestone:** App Shell, Navigation, Operability & Review Remediation
**Branch:** `feature/m10.5-app-shell-and-operability`
**Date:** 2026-07-12
**Author:** autonomous implementation run (Claude Code)

> Full plan: `project-plan/10.5-app-shell-and-operability.md`. This report records
> the open-question rulings, the AC-by-AC evidence, the CI-parity gauntlet output,
> the live-E2E results, and the backend-review items deferred to M11.

---

## 0. Open-question rulings (§16)

Asked at kickoff; answered by the owner:

- **OQ-1 → ×0.5.** The bear/long/sentiment<−0.5 sizing factor is **×0.5** (spec AC authoritative). `sizing.py`, the M08 §6.2 pseudocode intent, and the test now agree.
- **OQ-2 → enforce all three.** `max_concurrent`, `leverage_cap`, `permitted_asset_classes` are **enforced** (no migration; the fields already existed).
- **OQ-3 → unmount `/admin/` in prod.** Django admin mounts only under `DEBUG`.

---

## 1. Acceptance Criteria — evidence

| AC | Verdict | Evidence |
|---|---|---|
| **AC-10.5-1** From `/` a logged-out user reaches login by clicking; landing names the product + explains the flow with Sign in / Create account CTAs | ✅ | Live (Chrome, localhost:4444): `/` renders "DEVELOPMENT" badge + h1 + subtitle + 4-step "How it works" + disclaimer; clicking **Sign in** → `/login`. `landing.component.spec` asserts both CTAs navigate. |
| **AC-10.5-2** Persistent shell on every authed route; nav reaches dashboard/strategies/backtest/risk/orders/settings + staff-only admin, each reachable by click | ✅ | Live: authed `/dashboard` shows `banner` + `nav "Primary navigation"` with all 6 links + Getting-started; clicking **Orders** → `/orders`. `shell.component.spec` asserts nav links + Admin hidden for non-staff / shown for staff. |
| **AC-10.5-3** Sign out from the user menu; clears auth, tears down the WS, lands on `/` | ✅ | Live: user menu (`haspopup=menu`) → **Log Out** → `/`; `localStorage.stp_refresh_token === null` after. `AuthFacade.signOut()` calls `ws.forceDisconnect()` + `clearAuth()` + `navigate(['/'])`. |
| **AC-10.5-4** `/` (authed) → `/dashboard`; unknown path authed → real 404 with link home; anon → `/login?next=<path>` | ✅ | Live: authed `/totally-bogus-authed-path` → **404 "Page not found"** + "Go home"→/dashboard; anon `/nonexistent-xyz` → `/login?next=/nonexistent-xyz`; authed `/` → `/dashboard` (landingGuard). `landing.guard.spec`, `not-found.component.spec`. |
| **AC-10.5-5** Brand-new dashboard shows the 4-step Getting-started checklist from real `GET /onboarding/status/`; each step deep-links; MFA is step 0 | ✅ | Live (MFA-enrolled E2E user): checklist shows **"Enable two-factor authentication → Done"**, then Connect→`/settings/brokers`, Add→`/strategies`, View→`/dashboard`. Backend `test_onboarding.py` (7 tests). `onboarding-checklist.component.spec`. |
| **AC-10.5-6** Every help article reachable; no `/help/:slug` 404s for a real slug; jargon has affordances | ✅ | Live: dashboard "?" next to Market sentiment → `/help/sentiment` renders the full sanitized article; `/help` index links all 13. `help-article.component.spec` (load/sanitize/404/traversal-block). |
| **AC-10.5-7** Two emails → two login-rate-limit buckets (C1) | ✅ | `apps/users/test_auth.py::RateLimitKeyTests` — with `RATELIMIT_ENABLE=True`, email A's 6th request is 429 while email B's first is not; keyer parses the body. |
| **AC-10.5-8** Prod boot raises on unset/insecure `SECRET_KEY`/`FERNET_KEK` (C2) | ✅ | `config/test_prod_settings.py::ProdKeyFailClosedTests` — subprocess prod import raises on missing FERNET_KEK + on the insecure SECRET_KEY default; a properly-configured import succeeds. Prod-import smoke: `DEBUG=False`, keys required. |
| **AC-10.5-9** MFA step-up path is rate-limited → lockout + audit event (C3) | ✅ | `apps/users/test_mfa.py::MfaStepUpThrottleTests` — 5 bad codes → audit `security.mfa_stepup_throttled`; 6th rejected pre-check even for a valid code; a good code before the cap resets. |
| **AC-10.5-10** Revoke-other / password-change keep the caller's own family (SEC-4) | ✅ | `test_mfa.py::SessionsTests::test_revoke_all_other_sessions` (families A/B revoked, caller survives, `revoked == 2`), `test_password_change_preserves_current_session`, `test_list_sessions` (exactly one "current"). |
| **AC-10.5-11** Failed login does not logout/refresh, isn't double-submitted, renders a human message (C-FE-1 + C-FE-2) | ✅ | `refresh.interceptor.spec` — a 401 on `/auth/login/` does not call `refreshSession`/`logout` and propagates. `auth.login.error.UNKNOWN` added; `MissingTranslationHandler` + `missing-translation.handler.spec`. |
| **AC-10.5-12** Risk engine advertises no unenforced control; `requested_qty` respected (§7.7) | ✅ | `apps/risk/test_risk.py` — soft-stop fires on real intraday DD; `SIZING_ASSET_CLASS_BLOCKED` + `SIZING_MAX_CONCURRENT` rejects; `test_leverage_cap_clamps_notional`; `test_requested_qty_clamps_down` + `test_requested_qty_caps_order_qty`; `test_sentiment_cut_factor_is_half` (×0.5); `test_profile_get_returns_defaults_without_persisting`. |
| **AC-10.5-13** Labels; modal focus traps + Escape + restore; keyboard rows; text alternatives | ✅ | Security inputs labelled; `webhook-config`/`halt-platform` modals migrated to `<app-modal>` (focus trap + Escape + restore incl. `@if`-unmount via `ngOnDestroy`); orders drawer `role=dialog`+Escape+focus-in; orders/backtest rows `tabindex+role=button`+Enter/Space; regime band `role=img`+summary+legend; backtest canvas `aria-label`; PBO "high" text indicator. `modal.component.spec` (trap/Escape/restore). |
| **AC-10.5-14** Shell/landing/kit use tokens; no new `bg-blue-600` literals in milestone code; toasts replace swallowed-error sites | ✅ | UI kit + shell/landing use `bg-primary-*`/token utilities; `--space/--radius/--shadow` wired into Tailwind. `ToastService` + inline `role=alert` cover the 10 §7.5 sites. |

**All AC-10.5-1…14 pass.**

---

## 2. CI-parity gauntlet (§10.5)

Run 2026-07-12. **Note on the local runner:** the backend Docker image mounts only
`./backend:/app`, so (a) pytest must be pointed at `config.settings.test`
explicitly (the container env defaults `DJANGO_SETTINGS_MODULE=config.settings.dev`,
under which `RATELIMIT_ENABLE` is undefined and django-ratelimit defaults it to
`True`), and (b) `config/test_alert_rules.py` can't see `infra/` and its 5 tests
fail locally — they pass in CI where the full repo is checked out. Both are
environment artifacts, not regressions.

**Backend**
- `pytest -q` (SQLite, `config.settings.test`) — **green** (excluding the 5 `test_alert_rules.py` infra-path tests that require the repo root; green in CI).
- `pytest -m pg` (`config.settings.test_pg`, Postgres) — **8 passed**.
- `ruff check .` — **All checks passed.**
- `bandit -r apps/ config/ -x tests -q --severity-level medium` — **clean** (only informational `nosec` notes).
- `python manage.py makemigrations --check` — **No changes detected** (migration `audit.0004` committed).
- prod-import smoke — imports `config.settings.prod` with real `SECRET_KEY`/`FERNET_KEK`/`DATABASE_URL`: `DEBUG=False`, `METRICS_REQUIRE_AUTH=True`, fail-closed `/metrics` warning fires.

**Frontend**
- `npx ngc --noEmit -p tsconfig.app.json` — **clean**.
- `ng build` — **bundle generation complete** (shell chunk builds; all lazy routes intact).
- `ng test --no-watch --browsers=ChromeHeadless` — **99 SUCCESS** (0 failures).
- OpenAPI schema regenerated (`docs/openapi/openapi.json` +onboarding path) → `frontend/src/app/core/generated/schema.ts` regenerated.

---

## 3. Live E2E (§10.4) — Chrome, localhost:4444

1. Anonymous `/` → click **Sign in** → `/login`. ✅
2. Authenticated (MFA-enrolled test user via injected refresh token) → lands in the shell; nav present; **Orders** click → `/orders`. ✅
3. User menu → **Log Out** → `/`; `stp_refresh_token` cleared; landing shows Sign in. ✅
4. Getting-started checklist shows **MFA → Done** + Connect/Add/View deep-links reflecting real state. ✅
5. Authed `/totally-bogus-authed-path` → real **404** + Go-home→/dashboard; anon `/nonexistent-xyz` → `/login?next=/nonexistent-xyz`. ✅
6. Help **?** (Market sentiment) → `/help/sentiment` renders the full article. ✅

Console: only the pre-existing `ng serve` `/config.js` 404 (nginx-injected runtime config absent under `ng serve`; `ConfigService` falls back to `development`). No M10.5-introduced errors.

---

## 4. INERT ledger + `vectorbt`

The honest INERT ledger is in `project-plan/PROGRESS.md` ("INERT features (M10.5 audit)"): regime plane, sentiment, TradeStation, backtest worker, Alpaca missed-fill recovery; soft-stop **resolved** here (RISK-1). **`vectorbt==1.0.0` operator check:** confirmed installed + importable in the backend image on 2026-07-12 (`python -c "import vectorbt"` → `1.0.0`).

`seed_tickers` management command runs: **92 tickers, 174 aliases** seeded (S&P-500 large-cap core).

---

## 5. Deferred to M11 (backend review M2–M11, §7.6 note)

Logged, NOT fixed here (not release-blocking for an operability milestone; several touch the order hot path and want the load-test harness first):

- **M2** webhook body-size check ordering.
- **M3** idempotency-before-halt ordering.
- **M4** `push_to_user` inside the locked transaction.
- **M7** audit advisory-lock contention under load.
- **M8** refresh-family race.
- **M9/M10/M11** Decimal-in-Alpaca-mapping, reconcile fan-out, and the remaining H/M items.
- **Backtest worker** (`SERVICE_ROLE` dispatch) — M11 §7.0 (BUG-011).
- **Regime/sentiment real data** — operator API keys + backfill + real scorers.
- **Alpaca missed-fill recovery (AC-04-11)** — needs the real Alpaca activities endpoint.

---

## 6. Commits (feature branch)

```
fix(security): §7.6 backend security track (C1/C2/C3/SEC-4/H8/M1)
fix(risk): §7.7 risk-engine truthfulness (RISK-1..5)
feat(onboarding): §7.3/§9 GET /api/v1/onboarding/status/
feat(ui): §7.4 shared UI kit + global ToastService
feat(shell): §7.1/§7.2 app shell, route restructure, landing, 404
fix(fe): §7.5 frontend correctness (C-FE-1..4)
fix(fe): §7.5 surface swallowed errors
feat(fe): §7.3 onboarding checklist, help viewer, honest dashboard
docs(m10.5): INERT ledger, corrections, ADR-043, README nav, CHANGELOG
fix(a11y): §7.8 accessibility on touched screens
```

**Not done autonomously:** branch not pushed; no PR opened; tag `v0.10.5-app-shell`
not created — deferred to the operator per the standing "commit/push only when
asked" rule. The branch is ready for `git push -u origin
feature/m10.5-app-shell-and-operability` + PR.
